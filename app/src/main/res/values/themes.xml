<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StartProjeect" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="background">@color/test_back</item>
        <item name="android:statusBarColor">@color/cream_back</item>
        <item name="android:navigationBarColor">@color/cream_back</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
<!--        <item name="android:windowTranslucentStatus">true</item>-->
<!--        <item name="android:windowTranslucentNavigation">true</item>-->

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="OnBoarding" parent="Base.Theme.StartProjeect">
        <item name="android:statusBarColor">@color/light_blue</item>
        <item name="android:navigationBarColor">@color/end_blue</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="background">@drawable/background_color</item>
    </style>
    <style name="mainscreens" parent="Base.Theme.StartProjeect">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="background">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>


</resources>